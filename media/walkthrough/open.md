# Open the sidebar

Click the π icon in the activity bar, or press `Ctrl+Alt+P` (`Cmd+Alt+P` on a Mac). The same shortcut toggles back to the editor.

Pi runs in your first workspace folder. Drop files on the terminal to mention them, paste a screenshot to attach it, and Ctrl+click a path in the output to open it.
