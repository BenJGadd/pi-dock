# Pi

Pi Dock runs whatever `piDock.command` says, `["pi"]` by default, in your workspace folder, with your own Pi configuration, extensions and skills. Install Pi so that `pi` works in a terminal, or set the command to its full path.

Pi is at [pi.dev](https://pi.dev).
